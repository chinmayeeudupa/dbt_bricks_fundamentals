version: 2

models:
  - name: silver_gbu_daily_health
    tests:
      - check_dropped_recs:
            model_name: ref('silver_gbu_daily_health')
            config:
              severity: warn
              warn_if: ">10000" 
